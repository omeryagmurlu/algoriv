* ~~Snaps API should be up and running~~
* ~~Need some helpers to handel the job of `typee`~~
* ~~Find a better name for `modules.js` and its default export `Modules`~~
* IMPLEMENT graph changing using sigma:
	* ~~a click on empty area -> add node~~
	* ~~a click on a node and then on an another node -> add edge~~ (if edges are weighted, ask by means of a popup)
* ~~Make a class which has helper methods to generate an `AlgorithmPrototype` in an imperative way!~~ !DONE
* WRITE SOME FUCKING TESTS ASSHOLE
* Rewrite InformationDemandingButton, it is implemented poorly
* Make getting frames out of logic async, and move it to a webworker
